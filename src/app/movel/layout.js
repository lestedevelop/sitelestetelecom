export const metadata = {
    title: "5G da Leste | Leste Móvel",
    description: "Leste Telecom",
};

export default function MovelLayout({ children }) {
    return <div className="bg-light">{children}</div>;
}

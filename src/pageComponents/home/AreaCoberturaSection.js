import Section from "@/components/layout/Section";
import {Title} from "@/components/typoraphy";

export default function AreaCoberturaSection(){
    return (
        <Section style={{background: 'green'}}>
            <Title text={'Área de cobertura section'}/>
        </Section>
    )
}